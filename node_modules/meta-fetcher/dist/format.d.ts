/**
 * Prefix the site host to the relative favicon paths
 * @param host | website host
 * @param path | favicon relative path
 */
declare const createValidUri: (host: string, path: string) => string;
export default createValidUri;
